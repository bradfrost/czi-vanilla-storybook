import React from 'react';
import renderer from 'react-test-renderer';
import {{pascalCase name}} from '../{{pascalCase name}}';

// Snapshot tests are handled via storyshots, so all unit tests
// captured here are for functionality, events, and any other tests
// not captured by storyshots